import { FC } from 'react';
import { Button } from 'antd';

// eslint-disable-next-line react/function-component-definition
const AccountSettings: FC = () => (
  <div>
    <Button>AccountSettings</Button>
  </div>
);
export default AccountSettings;
